apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: document-service
  labels:
    app: document-service
spec:
  type: ClusterIP
  selector:
    app: document-service
  ports:
    - name: http
      port: 8080
      targetPort: http
